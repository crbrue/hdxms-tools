# Contributing

1. Create a branch from `main`.
2. Add or update tests for every behavior change.
3. Run `pytest -q` before committing.
4. Do not change the canonical SI-table columns, column order, or row-order rules without an explicit output-contract version change.
5. Peptide QC remains diagnostic unless a future, separately documented exclusion policy is approved.
